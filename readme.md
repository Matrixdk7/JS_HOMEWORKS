### Homework #5-1

Conditions. Use Ternary Operators whenever possible.

- If variable a is zero, print 'True', otherwise print 'False'.
- If variable a is greater than zero, print 'True', otherwise print 'False'.
- If variable a is less than zero, print 'True', otherwise print 'False'.
- If variable a is greater than or equal to zero, print 'True', otherwise print 'False'.
- If variable a is less than or equal to zero, print 'True', otherwise print 'False'.
- If variable a is not zero, print 'True', otherwise print 'False'.
- If variable a is 'test', print 'True', otherwise print 'False'.
- If variable a is '1' in both value and type, then print 'True', otherwise print 'False'.
- If variable a is greater than zero and less than 5, then print 'True', otherwise print 'False'.
- If variable a is zero or equal to two, add 7 to it, otherwise divide it by 10. Print the new value of the variable to the screen.
- If variable a is equal to or less than 1, and variable b is greater than or equal to 3, then print the sum of these variables, otherwise print their difference (the result of subtraction).
- If variable a is greater than 2 and less than 11, or variable b is greater than or equal to 6 and less than 14, then print 'True', otherwise print 'False'.
- The variable num can take 4 values: 1, 2, 3, or 4. If it has the value 1, then we will write winter in the variable result, if it has the value 2 - spring, and so on. Solve the problem using `switch-case`.