### Homeworks #3-1

Script:

- Asks the user for his age via a `prompt`.
- Converts the entered value to a number (use `Number` or `unary +`).
- If the user entered a non-number (NaN, empty string, canceled the input) — output via alert: "Incorrect age".
- If everything is ok — output the message: "In 5 years you will be: X", where X is age + 5.

Task 2:

For each line, try to get a number in two ways:
- parseInt(...)
- parseFloat(...)

Print the result for each line and each function to the console.
Explain in the code comments why parseInt/parseFloat return a number for some values ​​and NaN for others.