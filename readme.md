### Homework #1-1

A simple program with the following conditions:

- Use `prompt` to ask for the user's name.
- Use `alert` to print `"Hello, John! How are you?"`, where `John` is the name entered by the user.
