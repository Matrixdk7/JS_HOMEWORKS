###Homework #12-1

Goal: to work out event.target, createElement, append, remove.

UI:

`<input>` + Add button
`<ul class="list">` with three items

Logic:

- Add button: adds a new li with the text from the input
- There is a ✖ button inside the li
- Clicking on li → toggles selected
- Clicking on ✖ → removes the element

Criteria:

- A single handler on ul
- Works for new elements
- ✖ does not trigger selected