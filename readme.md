### Homework #6-1

Implement the function `generateKey(length, characters)`, which returns a string of random characters from the set characters of length `length`.

<pre> const characters = 'abcdefghijklmnopqrstuvwxyz0123456789'; 
const key = generateKey(16, characters); 
console.log(key); // eg599gb60q926j8i </pre>