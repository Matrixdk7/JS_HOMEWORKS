### Homework #7-1

You need to write your own versions of the functions `indexOf`, `lastIndexOf`, `find`, `findIndex`, `includes`, `every` and `some`. In the same way as we wrote in the lesson.

Requirements:

1. The use of auxiliary methods of arrays is prohibited, if you need something, you need to write it yourself. Or take it from the lesson files.
2. All functions must be in separate files
3. In the file with each function there must be 2 examples of its use
4. In the file with each function, above its code, there must be a JSDoc description (you can generate it through AI after writing the function, or write it yourself.)