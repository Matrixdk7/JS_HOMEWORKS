### Homework #11-1

We have a tree structured as nested ul/li lists.

Write code that prints each list item to the console:

- Use a for..of loop

- Print the total number of items to the console

- Extract the text from the li tags and write the text contained in each item to an array - print this array to the console