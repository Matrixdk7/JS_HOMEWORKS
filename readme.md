### Homework #5-3

Given an array of numbers.
1. Using a loop, find the sum of the elements of this array, output it to the console.

`const arr = [1, 2, 3, 4, 5];`

2. Using a loop, find the sum of the squares of the elements of this array.

To square, you can use Math.pow() or the ** operator

** - number ** degree where number is the number being raised to a power, and degree is the power itself