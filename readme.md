### Homework #3-2

Task 1:

- Create separate variables `name`, `age`, `city`, `job` through destructuring.
- Create a new `shortInfo` object that contains only name and city.
- Create a new `renamed` object in which the keys will be renamed

Task 2:
- Form a c array without methods and without loops

Task 3:

- Create a variable `firstPersonName` → the name of the first person.
- Create a variable `oldest` to store the oldest person object
- Create an object `ageSummary`

Task 4:
- Create a variable `totalPrice`, where you calculate the amount
- Add the third product to the new array `updatedCart` (manually only)
- Create a variable `itemNames`, where as a string: "Book, Laptop, Pen"