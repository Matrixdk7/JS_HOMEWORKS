### Homework #12

Goal: to practice `dataset`, `closest`, `delegation`, `classList`.

Requirements:

- There are 3–5 text cards on the page
- Each has short (visible) and long (hidden) text
- Button with data-read-more-btn

Click:
- shows/hides long text
- changes the text of the button “Read more” ⇄ “Hide”
- adds/removes the expanded class
- Single handler — on the container

Criteria:

- Works for any number of cards
- closest() used
- No innerHTML