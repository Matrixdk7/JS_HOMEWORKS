### Homework #1-2

A simple page that, upon loading, asks the user to enter their name and confirm whether they want to see a greeting. Based on the response, it outputs an `alert()`.

#### Used:
- `alert`
- `confirm`
- `prompt`
- `if`